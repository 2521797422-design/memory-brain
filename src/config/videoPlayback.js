/** Viewer overlay caps — re-encode sources to ≤720p MP4/H.264 when possible. */
export const VIDEO_VIEWER_MAX_HEIGHT_PX = 720
export const VIDEO_VIEWER_MAX_WIDTH_PX = 1280

/** Background WebGL target FPS while a memory video is playing. */
export const SCENE_THROTTLE_FPS = 15
